package com.kaviarasu.bootstrapped_backend.Q52.models;

import jakarta.persistence.Entity;

@Entity
public class PayLater extends PurchaseDetails {
    // No additional fields

    // Optional override to keep consistency (not strictly needed unless custom column name is required)
}


